spring.application.name=blog

# MySQL DB connection
spring.datasource.url=jdbc:mysql://localhost:3306/blogdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=mohitsshetty986
spring.datasource.password=Travalue#8
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Show SQL queries
spring.jpa.show-sql=true

# Log parameter bindings (show actual ? values)
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.orm.jdbc.bind=TRACE

# Format the SQL nicely
spring.jpa.properties.hibernate.format_sql=true